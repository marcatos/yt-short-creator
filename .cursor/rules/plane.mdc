---
description: Plane is the canonical tracker for yt-short-creator
alwaysApply: true
---

# Plane tracker — YT short creator

Workspace `personal`. This folder is Plane project **YTSC** (`yt-short-creator`).
LAN `http://10.100.30.87`. UI `https://plane.marcatos.ddns.net`.

## Canonical owners

- Work → Plane work items (labels feat/fix/chore/known-issue/backlog)
- Session notes → comment on the work item (do not add git handoffs)
- Live facts / STATUS → Outline document **STATUS** (see `.cursor/rules/outline.mdc`)
- Decisions → Outline document **DECISIONS** (append at top)
- Product knowledge → Outline collection for this project
- Changelog → conventional commits
- Code → git

List open work items for `YTSC` at session start. Create a work item before coding.
If MCP create fails (`title` vs `name`), run:

```powershell
python C:\Users\simot\Documents\Projects\opnsense-server\scripts\plane_issue.py create --project YTSC --title "..."
```

Do not use Linear or GitHub Issues. Do not put secrets on Plane. Do not treat git STATUS/TODO/ROADMAP/DECISIONS as live.
